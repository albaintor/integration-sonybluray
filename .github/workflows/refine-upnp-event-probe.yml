name: Refine UPnP event probe

on:
  push:
    branches: [new_models]
    paths:
      - '.github/scripts/refine_upnp_event_probe.py'
      - '.github/workflows/refine-upnp-event-probe.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: new_models
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply refinement
        run: python .github/scripts/refine_upnp_event_probe.py
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          if [ -f test-requirements.txt ]; then pip install -r test-requirements.txt; fi
      - name: Format probe
        run: python -m black src/playback_probe.py --line-length 120
      - name: Verify
        run: |
          python -m compileall -q src tests
          python -m unittest discover -s tests -p 'test*.py'
          python -m pylint src
          python -m flake8 src --count --show-source --statistics
          python -m black src --check --diff --verbose --line-length 120
      - name: Commit refinement and cleanup
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm .github/scripts/refine_upnp_event_probe.py .github/workflows/refine-upnp-event-probe.yml
          git add src/playback_probe.py
          git commit -m 'Refine UPnP event transition diagnostics'
          git push origin HEAD:new_models
